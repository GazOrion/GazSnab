/** true — показать сразу (для отладки). false — через CONSULTATION_POPUP_DELAY_MS. */
export const CONSULTATION_POPUP_ALWAYS_VISIBLE = false;

/** Показ виджета консультации через 2 минуты после захода на сайт. */
export const CONSULTATION_POPUP_DELAY_MS = 2 * 60 * 1000;

export const CONSULTATION_POPUP_STORAGE_KEY = "gazsnab_consult_popup_closed";

/** Файл: `media/vidget.png` → `npm run sync:promo` */
export const CONSULTATION_POPUP_IMAGE_SRC = "/media/vidget.png";
